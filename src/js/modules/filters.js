// import { generateElement, generateButton, generateTextbox } from './render-templates'

// export function Filter(filterExpression, className = 'filter', text = '') {
// 	const filterId = `filter-${Date.now()}`
// 	const element = generateButton(className, text)

// 	return {
// 		id: filterId,
// 		renderControl: () => element,
// 		addListener: (type) => {

// 		}
// 	}
// }